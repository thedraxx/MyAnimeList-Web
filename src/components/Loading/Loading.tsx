import React from 'react'
import { ContainerSpinner, Spinner } from './Style'

const Loading = () => {
  return (
    <ContainerSpinner>
    <Spinner />
    </ContainerSpinner>
  )
}

export default Loading